# 前端页面剩余任务

状态：完成  
日期：2026-06-21

## 1. AI 生成页闭环

目标：让“生成候选题 -> 批量确认 -> 入库”像真实产品流程。

- [x] 生成中、生成完成、生成失败状态。
- [x] 候选题支持多选、取消选择、跳过。
- [x] 候选题支持单题轻编辑：题干、题型、选项、答案、标签。
- [x] 批量确认后页面显示已入库状态，不要每题都跳详情。
- [x] 入库后的题目能在题库页出现。

## 2. 资料上传页状态

目标：让资料页看起来能处理简历、JD、项目笔记和复习资料。

- [x] 选择文件后的文件队列。
- [x] 上传中、解析中、解析失败、已解析状态。
- [x] 资料详情展示解析出的知识点、项目点、可生成题目方向。
- [x] 资料详情能跳到 AI 生成页，并带出资料来源状态。

## 3. 题库管理

目标：让题库像“用户自己的题库”，不是静态列表。

- [x] 筛选条件在前端生效：方向、难度、状态、题型。
- [x] 空结果状态。
- [x] 单题编辑页或编辑区。
- [x] 删除、归档、恢复。
- [x] 批量选择和批量操作。

## 4. 练习闭环

目标：练完一题后能自然回到复习计划。

- [x] 问答题空回答提示。
- [x] 选择题选中态、提交态、正确/错误状态。
- [x] 结果页增加：下一题、回题库、标记掌握、加入错题、下次复习。
- [x] Dashboard 能展示最近练习和待复习变化。

## 5. Dashboard 工作台

目标：从展示页变成每天打开后的操作入口。

- [x] 今日复习队列。
- [x] 最近练习。
- [x] 错题和薄弱点入口。
- [x] AI 接入状态入口。
- [x] 生成/入库/练习后的状态能反映到 Dashboard。
- [x] 增加近 7 天练习趋势和薄弱点分布图表，使用 Recharts，避免继续手写复杂图表样式。

### Dashboard 后续图表增强

- [ ] 接入真实练习数据后，补充图表交互：tooltip、时间范围筛选、题型筛选。
- [ ] 在现有 Recharts 基础上补充缩放、悬浮详情、响应式坐标轴和多指标切换。
- [ ] 图表库只用于数据可视化，不替换现有 Tailwind 页面布局和基础组件体系。

## 6. 设置页细节

目标：设置页具备基本表单反馈。

- [x] 头像上传预览。
- [x] 保存成功/失败提示。
- [x] 邮箱验证码倒计时。
- [x] 密码重复不一致提示。
- [x] AI 配置测试连接结果。

## 7. 本次清理记录

目标：只删除确定不属于当前根目录 Next.js 原型源码的缓存、空壳和临时产物。

- [x] 删除根目录 `.next` 构建产物。
- [x] 删除根目录 `.npm-cache` 临时 npm 缓存。
- [x] 删除旧 `apps` 目录残留；当前项目源码已迁移到根目录 `src`。
- [x] 删除空的 `prototype` 目录。
- [x] 删除空上传占位目录 `uploads`，前端 mock 上传不需要真实落盘目录。
- [x] 删除 `tsconfig.tsbuildinfo` TypeScript 增量缓存。
- [x] 保留 `.reference`、`docs`、`migrations`，这些仍可作为参考、文档和后续 Postgres 设计材料。

## 建议顺序

1. 先做 AI 生成页闭环。
2. 再做资料上传状态。
3. 然后做题库管理。
4. 最后补练习回流和 Dashboard 联动。
